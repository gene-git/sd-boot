#
# /usr/lib/kernel/install.conf.d/010-sd-boot-install.conf
#
# Base kernel-install config
# Every kernel installed with kernel-install uses these settings:
# 
layout=uki
initrd_generator=dracut
uki_generator=ukify
